# nvim

Roni's neovim config

Currently tested with [NVIM v0.12.4](https://github.com/neovim/neovim/releases/tag/v0.12.4)

## Initial setup

First launch `nvim` to clone the deps, then run `bash setup.sh` to build the native dependencies.

